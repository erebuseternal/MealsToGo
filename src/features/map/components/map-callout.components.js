import React from 'react';
import { RestaurantCompactDetail } from '../../../components/restaurant/restaurant-compact-detail.components';

export function MapCallout({ restaurant }) {
  return <RestaurantCompactDetail restaurant={restaurant} />;
}
