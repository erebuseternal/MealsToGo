import React from 'react';
import { View, Text } from 'react-native';

import { AccountBackground, ButtonArea } from '../components/account.components';

export function RegisterScreen() {
  return (
    <AccountBackground>
      <Text>Please create an account.</Text>
    </AccountBackground>
  );
}
